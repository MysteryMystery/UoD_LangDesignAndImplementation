class Integer {
    -
    Int boxed = 0;

    +
    meth add expects Int other {
        boxed = boxed + other;
    }

    +
    meth set expects Int i {
        boxed = i;
    }

    +
    meth get {
        boxed;
    }
}

Integer x = new Integer;
x->set(4);
print x->get();